/**
 * Player
 * The Player class should minimally contain: connectionId (possibly you called it playerId), reference to current room, and a list of inventory items.
 */
public class Player {
    public String playerID = new String();
    public String currentRoom = new String();
    //public String[] InventoryItems = new String[inventory];

    public 
}